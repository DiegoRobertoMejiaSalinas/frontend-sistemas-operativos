mutation createDirectory($input: CreateDirectoryInput) {
  createDirectory(directory: $input) {
    directory {
      id
      name
      slug
      readableRoot
      writableRoot
      readableUser
      writableUser
      readableGuest
      writableGuest
    }
  }
}
