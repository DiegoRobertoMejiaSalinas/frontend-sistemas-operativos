export default function({ app, redirect }) {
    console.log(app.$store);
    // const isLogged = app.$store.state.localStorage.userId !== null;
    // if (!isLogged) {
    //   redirect("/login");
    // }
  }
  